<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /> <!-- For Android 9 (API level 28) and earlier -->
    <uses-permission
        android:name="android.permission.BROADCAST_CLOSE_SYSTEM_DIALOGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.READ_LOGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- FOREGROUND_SERVICE_CONNECTED_DEVICE is required once head tracking ships (the service
         will need to run as foregroundServiceType="connectedDevice" to stay connected to a
         head-tracking BLE device). The head tracking code is all in place, but the feature is
         currently disabled for users, so this permission is left out to avoid requiring it
         before it's needed. Re-add it, and "connectedDevice" to the service's
         foregroundServiceType below, when the feature ships. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".SoundscapeApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SoundscapeAlpha"
        tools:targetApi="31"
        tools:ignore="UnusedAttribute">
        <activity
            android:name=".OnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.SoundscapeAlpha" />
        <!-- Portrait lock is intentional: this is an accessibility/screen-reader-focused
             app, and an unexpected layout change from an in-pocket rotation is disruptive
             for the target audience. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="userPortrait"
            android:launchMode="singleTask"
            android:theme="@style/SplashTheme.Soundscape"
            tools:ignore="LockedOrientationActivity,DiscouragedApi">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <!-- Handle sharing of Google Maps URLs for beacons -->
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>

            <intent-filter>
                <!-- intents to load route from GPX file -->
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="content" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>

            <intent-filter>
                <!-- intents to load route from JSON file -->
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="content" />
                <data android:mimeType="application/json" />
            </intent-filter>

            <intent-filter>
                <!-- geo intents to set a beacon -->
                <data android:scheme="geo" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
            </intent-filter>

            <intent-filter>
                <!-- soundscape intents to set a street preview location -->
                <data android:scheme="soundscape" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="https"
                    android:host="links.soundscape.scottishtecharmy.org"
                    android:pathPrefix="/v1/sharemarker" />
            </intent-filter>
        </activity>

        <service
            android:name=".services.SoundscapeService"
            android:stopWithTask="true"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="location|mediaPlayback"
            android:permission="android.permission.FOREGROUND_SERVICE_LOCATION">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>
        <!-- AppFunctions: lets Gemini and other agents call Soundscape's actions. The
             service class and the schema XML are generated by KSP from the annotations in
             BaseSoundscapeAppFunctionService. API 36+, so it simply never starts on older
             devices - the app's minSdk is 30.

             Note this is a *separate* service from SoundscapeService above: the system
             binds to it without the foreground location service running, which is why the
             functions needing audio report "Open Soundscape first" rather than trying to
             start one from the background. -->
        <service
            android:name=".appfunctions.SoundscapeAppFunctionService"
            android:exported="true"
            android:permission="android.permission.BIND_APP_FUNCTION_SERVICE"
            tools:targetApi="36">
            <property
                android:name="android.app.appfunctions.v2"
                android:value="soundscape_app_function_service.xml" />
            <intent-filter>
                <action android:name="android.app.appfunctions.AppFunctionService" />
            </intent-filter>
        </service>

        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="org.scottishtecharmy.soundscape.provider"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Disable screen_view events in Firebase -->
        <meta-data
            android:name="google_analytics_automatic_screen_reporting_enabled"
            android:value="false" />

        <!-- Disable Firebase auto-initialization at app startup -->
        <provider
            android:name="com.google.firebase.provider.FirebaseInitProvider"
            android:authorities="${applicationId}.firebaseinitprovider"
            tools:node="remove" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
    </queries>

</manifest>